CREATE TABLE person (
    id INT PRIMARY KEY NOT NULL,
    name VARCHAR(20) NOT NULL,
    surname VARCHAR(20) NOT NULL,
    age INT NOT NULL,
    birth_date DATE,
    address VARCHAR(25),
    salary REAL
);
